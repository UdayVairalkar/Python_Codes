"""data may diff types to gether """
studen=["uday vairalar",61,"buldhana"]
print(studen)
print(studen[0])
studen[0]="yudi"
print(studen[0])
"""strings are immutable we can change 
the strings by using the index accessing like we done in this program """